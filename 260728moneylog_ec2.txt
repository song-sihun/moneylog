# 1. EC2에 디렉토리 생성
ssh -i E:/git_dir/260722moneylog/moneylog-key.pem ec2-user@54.180.119.180 "mkdir -p ~/app/frontend"

# 2. 백엔드 JAR 전송
scp -i E:/git_dir/260722moneylog/moneylog-key.pem E:/git_dir/260722moneylog/moneylog/moneylog-backend/build/libs/moneylog-backend-0.0.1-SNAPSHOT.jar ec2-user@54.180.119.180:~/app/app.jar

# 3. 프론트엔드 빌드 폴더(dist) 전송
scp -i E:/git_dir/260722moneylog/moneylog-key.pem -r E:/git_dir/260722moneylog/moneylog/moneylog-front/dist/* ec2-user@54.180.119.180:~/app/frontend/

DOCKER_BUILDKIT=0 docker compose up --build -d

ssh -i moneylog-key.pem ec2-user@54.180.119.180

docker system prune -a --volumes -f

# 1. 2GB 크기의 스왑 파일 생성
sudo dd if=/dev/zero of=/swapfile bs=128M count=16

# 2. 스왑 파일에 권한 설정
sudo chmod 600 /swapfile

# 3. 스왑 공간 생성
sudo mkswap /swapfile

# 4. 스왑 공간 활성화
sudo swapon /swapfile

# 5. 재부팅 시에도 유지되도록 fstab 등록
echo '/swapfile swap swap defaults 0 0' | sudo tee -a /etc/fstab

# 6. 적용 확인 (Swap 항목에 2.0G 정도가 생겼는지 확인)
free -h